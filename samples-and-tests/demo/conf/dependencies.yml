# Application dependencies

require:
    - play
    - play -> spring 1.0.1
    - springtester -> springtester 0.1

repositories:
    - My modules:
        type:       local
        artifact:   "${application.path}/../../../[module]-[revision]"
        contains:
            - springtester
